version: "3.8"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/.yarn/cache
    ports:
      - 3000:3000
